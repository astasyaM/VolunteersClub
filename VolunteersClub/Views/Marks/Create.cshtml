@model VolunteersClub.Models.Mark

@{
    ViewData["Title"] = "Create";
}

<div class="text-center">
    <h1>Оценивание работы волонтёра</h1>
</div>

<style>
    .card-container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 80vh; 
    }

    .card {
        width: 70%; 
        max-width: 600px; 
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1); 
        margin: auto; 

    }

    .form-group textarea.form-control {
        resize: vertical; 
    }
</style>


<hr />
<div class="card-container">
    <div class="card">
        <hr />
        <div class="row">
            <div class="col-md-12">
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group mb-3">
                        <label asp-for="CurrentMark" class="control-label">Оценка волонтёра</label>
                        <input asp-for="CurrentMark" class="form-control" />
                        <span asp-validation-for="CurrentMark" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="Notes" class="control-label">Комментарий к оценке</label>
                        <input asp-for="Notes" class="form-control" />
                        <span asp-validation-for="Notes" class="text-danger"></span>
                    </div>
                    <input type="hidden" name="additionalNumber" value="@ViewBag.ID">
                    <div class="form-group">
                        <input type="submit" value="Сохранить" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
